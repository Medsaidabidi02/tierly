PORT=3001

# The Kick Pusher app key (reverse-engineered). If Kick updates their infra,
# inspect your browser's Network tab on kick.com and filter for "pusher" to find the new key.
KICK_PUSHER_KEY=32cbd69e4b950bf97679

# Pusher cluster region — us2 is the one Kick currently uses
KICK_PUSHER_CLUSTER=us2
